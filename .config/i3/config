# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Modifier Keys

set $mod Mod4
set $alt Mod1

# Font

font pango:monospace 8

# Startup

exec_always --no-startup-id nitrogen --restore
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id picom --experimental-backends
exec --no-startup-id dunst
exec --no-startup-id emote
exec --no-startup-id mpd
exec --no-startup-id yay -Sy
exec --no-startup-id lxsession
exec --no-startup-id discord-canary
exec xrdb -merge .Xresources

# Keybindings

floating_modifier $mod

# start a terminal
bindsym $mod+Return exec kitty sh -c "neofetch && zsh"
bindsym $mod+$alt+Return exec kitty sh -c "micro $HOME/.config/i3/config" 
bindsym $alt+Return exec xterm

# start Discord
# change this to "discord" or "discord-ptb" if you use a different branch.
bindsym $alt+d exec discord-canary

# start Firefox
bindsym $alt+c exec firefox
bindsym $alt+Shift+c exec firefox --private

# kill focused window
bindsym $mod+c kill

# start rofi (a program launcher)
bindsym $mod+d exec --no-startup-id rofi -show drun
#bindsym $mod+Shift+d exec --no-startup-id rofi -show p -modi p:rofi-power-menu -width 20 -lines 6

# change focus
# if you change these, remember to change them at lines 66-69 and 204-207 as well.
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+l move right

# split in horizontal/vertical orientation
bindsym $mod+h split h
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# screenshot
bindsym $mod+Shift+s exec --no-startup-id flameshot gui -c

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# lock the screen
bindsym $alt+l exec $HOME/bin/lock

# emoji picker
bindsym $mod+period exec emote

# workspace setup
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
set $ws14 "14"
set $ws15 "15"
set $ws16 "16"
set $ws17 "17"
set $ws18 "18"
set $ws19 "19"
set $ws20 "20"

# uncomment/change this if you have multiple monitors and want to bind each one to a workspace.
# workspace 1 output DP-4
# workspace 2 output DP-4
# workspace 3 output DP-4
# workspace 4 output DP-4
# workspace 5 output DP-4
# workspace 6 output DP-4
# workspace 7 output DP-4
# workspace 8 output DP-4
# workspace 9 output DP-4
# workspace 10 output DP-4
# workspace 11 output HDMI-0
# workspace 12 output HDMI-0
# workspace 13 output HDMI-0
# workspace 14 output HDMI-0
# workspace 15 output HDMI-0
# workspace 16 output HDMI-0
# workspace 17 output HDMI-0
# workspace 18 output HDMI-0
# workspace 19 output HDMI-0
# workspace 20 output HDMI-0

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+$alt+1 workspace number $ws11
bindsym $mod+$alt+2 workspace number $ws12
bindsym $mod+$alt+3 workspace number $ws13
bindsym $mod+$alt+4 workspace number $ws14
bindsym $mod+$alt+5 workspace number $ws15
bindsym $mod+$alt+6 workspace number $ws16
bindsym $mod+$alt+7 workspace number $ws17
bindsym $mod+$alt+8 workspace number $ws18
bindsym $mod+$alt+9 workspace number $ws19
bindsym $mod+$alt+0 workspace number $ws20

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10
bindsym $mod+$alt+Shift+1 move container to workspace number $ws11
bindsym $mod+$alt+Shift+2 move container to workspace number $ws12
bindsym $mod+$alt+Shift+3 move container to workspace number $ws13
bindsym $mod+$alt+Shift+4 move container to workspace number $ws14
bindsym $mod+$alt+Shift+5 move container to workspace number $ws15
bindsym $mod+$alt+Shift+6 move container to workspace number $ws16
bindsym $mod+$alt+Shift+7 move container to workspace number $ws17
bindsym $mod+$alt+Shift+8 move container to workspace number $ws18
bindsym $mod+$alt+Shift+9 move container to workspace number $ws19
bindsym $mod+$alt+Shift+0 move container to workspace number $ws20

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3
bindsym $mod+Shift+r restart
# exit i3
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Really log out?' -b 'Yes, return to login screen.' 'i3-msg exit'"

# uncomment if using multiple monitors and want to assign discord to your secondary monitor
# assign [class="discord"] number 11

# resize window
mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym i resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# borders

## class                 border  backgr. text    indicator child_border                                                                     
client.focused_inactive #131416 #131416 #131416 #131416 #131416
client.unfocused        #131416 #131416 #131416 #131416 #131416                                                                            
client.focused          #FFFFFF #FFFFFF #FFFFFF #FFFFFF #FFFFFF  
for_window [class="^.*"] border pixel 3

# gaps

gaps inner 10
gaps outer 10
gaps top 30
